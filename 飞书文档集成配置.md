# 飞书文档集成配置指南

## 功能说明

通过飞书钉选机器人，将飞书文档内容同步到 Claude Code，实现：
- 读取飞书文档内容
- 写入/更新飞书文档
- 文档搜索和列表

## 前置准备

### 1. 创建飞书开放平台应用

访问 [飞书开放平台](https://open.feishu.cn/app)

1. **创建应用**
   - 类型：企业自建应用
   - 应用名称：Claude Code (或其他名称)
   - 记录应用 ID 和 App Secret

2. **配置应用信息**
   ```
   应用ID: cli_xxxxxx（例如：cli_a931609c20f8dbc0）
   App Secret: your_app_secret
   ```

### 2. 配置权限

在应用的「权限管理」中添加以下权限：

| 权限名称 | 权限描述 |
|----------|----------|
| `drive:drive:readonly` | 读取云文档 |
| `drive:drive:write` | 写入云文档 |
| `drive:file:readonly` | 读取文件 |
| `drive:file:write` | 写入文件 |
| `im:chat:readonly` | 读取会话消息 |
| `im:message:read` | 读取消息内容 |

### 3. 发布应用

1. 创建版本
2. 提交审核
3. 等待审核通过（通常 1-3 个工作日）

## Claude Code 配置

### 1. 配置文件位置

创建或编辑 `.cc-connect/config.toml`：

```toml
language = "zh"

[log]
level = "info"

[[projects]]
name = "my-project"

[projects.agent]
type = "claudecode"

[projects.agent.options]
work_dir = "/Users/a58/code"
mode = "default"

[[projects.platforms]]
type = "feishu"

[projects.platforms.options]
app_id = "cli_a931609c20f8dbc0"
app_secret = "t4VQN4r60GIrljfs2EFr5bs7l2JTxgkf"
```

### 2. 重启服务

配置后需要重启 Claude Code 或 CC Switch 服务。

## 使用方法

### 1. 在飞书文档中 @ 我

在任意飞书文档中输入：
```
@Claude Code
```

### 2. 使用技能命令

#### 获取文档内容
```bash
node skills/feishu-docs/bin/cli.js get \
  --document-id dcnxxxxxx \
  --include-content \
  --format markdown
```

#### 创建新文档
```bash
node skills/feishu-docs/bin/cli.js create \
  --folder-token fldxxxxxx \
  --title "项目计划" \
  --content "# 项目概述\n\n## 目标\n- 完成开发\n- 测试通过"
```

#### 更新文档
```bash
node skills/feishu-docs/bin/cli.js update \
  --document-id dcnxxxxxx \
  --content "# 更新内容"
```

#### 搜索文档
```bash
node skills/feishu-docs/bin/cli.js search --query "项目"
```

#### 列出文档
```bash
node skills/feishu-docs/bin/cli.js list --folder-token fldxxxxxx
```

## 钉选机器人配置

### 1. 创建钉选机器人

1. 进入应用的「钉选机器人」配置
2. 创建新的机器人
3. 配置回调地址（如需要）
4. 保存并启用

### 2. 配置接收消息权限

在应用的「消息与菜单」 -> 「接收消息」中：
- 勾选「接收群消息」
- 勾选「接收私聊消息」
- 勾选「接收@我的消息」

## 故障排除

### 1. 认证失败

**错误**: `invalid app_id or app_secret`

**解决**:
- 检查 `app_id` 和 `app_secret` 是否与开放平台一致
- 确认应用已发布

### 2. 权限不足

**错误**: `permission denied`

**解决**:
- 检查应用是否已获得必要权限
- 确认应用已发布
- 重新提交审核

### 3. 无法读取文档

**原因**:
- 应用未被添加到文档的访问权限中
- 文档不在应用的访问范围内

**解决**:
- 在文档中点击「分享」→「添加成员」→ 选择你的应用
- 确保文档在应用的租户范围内

## 技能文件位置

```
/Users/a58/.openclaw/workspace/skills/feishu-docs/
├── bin/
│   └── cli.js          # 命令行接口
├── src/
│   └── api.js          # API 客户端
├── package.json
└── README.md
```

## 相关配置文件

| 文件 | 说明 |
|------|------|
| `~/.cc-connect/config.toml` | CC Switch 配置 |
| `~/.claude/settings.json` | Claude Code 设置 |
| `~/.claude/.mcp.json` | Notion MCP 配置 |
| `/Users/a58/.openclaw/credentials/feishu-default-allowFrom.json` | 飞书权限白名单 |

## 许可证

MIT License
